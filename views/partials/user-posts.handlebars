<a href="/post/{{id}}">
  <div class="card">
    <div class="card-header">
      <h2>{{title}}</h2>
      <p class="user-details">Created by
        {{user.username}}
        on
        {{format_date date_created}}</p>
      <p>
        {{content}}
      </p>

    </div>
  </div>
</a>